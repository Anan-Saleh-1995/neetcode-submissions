class Solution {
    getConcatenation(nums: number[]): number[] {
        const numsLength = nums.length;
        const numsLengthTwice = numsLength * 2;
        let ans: number[];

        for (let i = 0; i < nums)

        return ans;
    }
}
